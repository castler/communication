{
  "name": "bazel-job",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action: Bazel job setup with main/post lifecycle for cache management",
  "scripts": {
    "build": "npm run build:main && npm run build:post",
    "build:main": "ncc build src/pre.js -o dist/main",
    "build:post": "ncc build src/post.js -o dist/post"
  },
  "dependencies": {
    "@actions/cache": "^4.0.0",
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
