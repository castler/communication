# *******************************************************************************
# Copyright (c) 2024 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************

name: "Address / UB / Leak Sanitizer"
description: "Runs all tests with --config=asan_ubsan_lsan to detect memory errors, undefined behavior, and leaks."

inputs:
  cache-mode:
    description: "Cache mode override (empty = auto-compute from event context)."
    default: ""
  max-retries:
    description: "Maximum number of attempts for the commands block."
    default: "1"
  fetch-only:
    description: "When true, adds --nobuild to skip compilation (fetch dependencies only)."
    default: "false"

runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/00_infrastructure/bazel_job
      with:
        disk-cache: build_and_test_asan_ubsan_lsan
        cache-mode: ${{ inputs.cache-mode }}
        max-retries: ${{ inputs.max-retries }}
        fetch-only: ${{ inputs.fetch-only }}
        commands: "bazel test --config=asan_ubsan_lsan //... --build_tests_only"
